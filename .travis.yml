language: objective-c
podfile: Demo/Podfile
osx_image: xcode7.3
sudo: false
before_install:
  - gem install cocoapods --no-rdoc --no-ri --no-document --quiet
script:
  - xctool test -scheme Realm+JSON -workspace Demo/RealmJSONDemo.xcworkspace -sdk iphonesimulator